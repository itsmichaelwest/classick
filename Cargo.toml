[workspace]
resolver = "2"
members = ["crates/classick"]

# Workspace defaults shared across all crates (currently just classick;
# additional members — e.g. crates/classick-core when the cross-platform
# split happens — will inherit these).
[workspace.package]
edition = "2021"
license = "MIT"
