<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="IpodSync_UI.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:IpodSync_UI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!--
        Settings shell. Size is fixed at 640x740 in code-behind; the root
        Grid does NOT carry Width/Height because SizeClientArea reads the
        constants from SettingsWindow.cs (kept there so the popover and
        settings can share the WindowAnchor helper consistently).

        Mica backdrop is set in code-behind via SystemBackdrop. NavView
        pane is pinned open at 200 DIP, the toggle button is hidden so
        users can't collapse it. There is no Save/Cancel footer — every
        SettingsCard control writes through the VM, which debounces and
        sends SaveConfig automatically.
    -->
    <Grid x:Name="SettingsRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar x:Name="SettingsTitleBar" Grid.Row="0" Title="ipod-sync Settings" />
        <NavigationView
            x:Name="Nav"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            IsPaneToggleButtonVisible="False"
            PaneDisplayMode="Left"
            OpenPaneLength="200"
            SelectionChanged="Nav_SelectionChanged">

            <!--
                Pane header hosts the multi-iPod chooser. Single-iPod
                today; the chooser surfaces all known iPods + "Add new…"
                as soon as the daemon learns about more than one.
            -->
            <NavigationView.PaneHeader>
                <Grid Padding="8,4,8,4">
                    <DropDownButton x:Name="IpodChooser"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Padding="12,6">
                        <DropDownButton.Content>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Grid.Column="0"
                                           Text="{x:Bind ViewModel.Chooser.SelectedDisplayName, Mode=OneWay}"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </DropDownButton.Content>
                        <DropDownButton.Flyout>
                            <MenuFlyout x:Name="IpodChooserFlyout" Placement="Bottom" />
                        </DropDownButton.Flyout>
                    </DropDownButton>
                </Grid>
            </NavigationView.PaneHeader>

            <NavigationView.MenuItems>
                <NavigationViewItem Content="General" Tag="general">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Notifications" Tag="notifications">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEA8F;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="History" Tag="history">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE823;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
