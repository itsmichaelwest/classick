<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IpodSync_UI.Views.ProgressPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:IpodSync_UI.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Grid Padding="32" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Sync in progress" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="{x:Bind ViewModel.Source, Mode=OneWay}"
                Opacity="0.7"
                TextWrapping="Wrap" />
        </StackPanel>

        <!-- Progress bar + counters -->
        <StackPanel Grid.Row="1" Spacing="8">
            <ProgressBar
                Minimum="0"
                Maximum="{x:Bind ViewModel.TotalPlanned, Mode=OneWay}"
                Value="{x:Bind ViewModel.Done, Mode=OneWay}"
                IsIndeterminate="False" />
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock>
                    <Run Text="{x:Bind ViewModel.Done, Mode=OneWay}" />
                    <Run Text=" of " />
                    <Run Text="{x:Bind ViewModel.TotalPlanned, Mode=OneWay}" />
                </TextBlock>
                <TextBlock Grid.Column="1">
                    <Run Text="{x:Bind ViewModel.PercentComplete, Mode=OneWay, FallbackValue=0}" />
                    <Run Text="%" />
                </TextBlock>
            </Grid>
        </StackPanel>

        <!-- Current track -->
        <Border Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <StackPanel Spacing="4">
                <TextBlock Text="Current track" FontWeight="SemiBold" Opacity="0.7" />
                <TextBlock
                    Text="{x:Bind ViewModel.CurrentTrackLabel, Mode=OneWay}"
                    TextWrapping="NoWrap"
                    TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Border>

        <!-- Log tail -->
        <Border Grid.Row="3"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <Grid RowDefinitions="Auto,*">
                <TextBlock Grid.Row="0" Text="Log" FontWeight="SemiBold" Opacity="0.7" Margin="0,0,0,8" />
                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.LogTail, Mode=OneWay}"
                          SelectionMode="None"
                          IsItemClickEnabled="False"
                          ScrollViewer.HorizontalScrollMode="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:LogLine">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                <TextBlock Grid.Column="0"
                                           Text="{x:Bind Timestamp.LocalDateTime, Mode=OneTime}"
                                           Opacity="0.5"
                                           FontFamily="Consolas" />
                                <TextBlock Grid.Column="1"
                                           Text="{x:Bind Message, Mode=OneTime}"
                                           TextWrapping="Wrap"
                                           FontFamily="Consolas" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Finish state -->
        <StackPanel Grid.Row="4" Spacing="12" Visibility="{x:Bind ViewModel.IsFinished, Mode=OneWay}">
            <InfoBar IsOpen="True"
                     Severity="Success"
                     Title="Done"
                     Message="{x:Bind ViewModel.FinishMessage, Mode=OneWay}" />
            <Button Content="Close" HorizontalAlignment="Right" Command="{x:Bind ViewModel.CloseCommand}" />
        </StackPanel>
    </Grid>
</Page>
